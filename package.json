{
  "name": "applepay-shopify-cloudflare",
  "private": true,
  "workspaces": [
    "apps/control-plane",
    "apps/admin-ui"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260115.0",
    "concurrently": "^9.1.0",
    "wrangler": "^4.60.0"
  },
  "scripts": {
    "dev": "concurrently -n UI,WORKER -c auto \"npm -w apps/admin-ui run dev\" \"npm -w apps/control-plane run dev\"",
    "build": "npm -w apps/admin-ui run build && npm -w apps/control-plane run build",
    "deploy": "npm -w apps/admin-ui run build && npm -w apps/control-plane run deploy",
    "postinstall": "node ./scripts/ensure-rollup-native.mjs"
  }
}
